{
  "$schema": "https://json.schemastore.org/mcp.json",
  "mcpServers": {
    "fetch": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-fetch"],
      "description": "Enhanced web fetching with better compatibility"
    }
  }
}
